// Write your code here
import './index.css'

const LatestMatch = props => {
  const {latestMatchs} = props
  const {competingLogo, competingTeam} = latestMatchs

  return <h1>dnv</h1>
}

export default LatestMatch
